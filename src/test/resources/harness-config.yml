inputFormat: xml
context: testContext

databasesUnderTest:

  - name: cassandra
    version: 3.11
    url: jdbc:cassandra://localhost:9042;DefaultKeyspace=betterbotz
    username: cassandra
    password: cassandra
    dbSchema: betterbotz

# have to run 4.0 on Centos VM because it's dying on Windows 10 Docker Desktop 3.3.3
  - name: cassandra
    version: 4
    url: jdbc:cassandra://192.168.1.138:9043;DefaultKeyspace=betterbotz
    username: cassandra
    password: cassandra
    dbSchema: betterbotz
