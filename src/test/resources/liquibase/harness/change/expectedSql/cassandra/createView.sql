INVALID TEST
-- Cassandra doesn't support simple view, only materialized views, but that's another changeType that should have separate test case
-- as a reference - materialized views are supported for oracle extension