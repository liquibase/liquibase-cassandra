INVALID TEST
-- Cassandra doesn't support unique constraints as standalone structure
-- as workaround columns can be added to primary key to make values unique, but that's not what we are testing
-- https://stackoverflow.com/questions/8154332/cassandra-unique-constraint-on-row-key